/**
 * @chat-hub/shared-types
 *
 * TypeScript interfaces/DTOs shared between chat-hub-api and chat-hub-web.
 * Intentionally empty in this scaffolding session — real domain types
 * (Organization, Site, User, Role, Conversation, etc.) land in a later
 * session once entities are designed. This file only proves the package
 * is wired up and importable from both apps.
 */

export const SHARED_TYPES_PACKAGE_VERSION = '0.0.1';
